<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.iitp.mayank.celesta2k17">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".activities.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".activities.MainActivity"
            android:label="@string/app_name"
            android:launchMode="singleTop"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <activity
            android:name=".activities.EventsActivity"
            android:label="@string/events">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".activities.MainActivity" />
        </activity>

        <activity
            android:name=".activities.MyProfile"
            android:label="@string/my_profile">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".activities.MainActivity" />
        </activity>
        <!-- <activity -->
        <!-- android:name=".fragments.GalleryFragment" -->
        <!-- android:label="@string/gallery"> -->
        <!-- <meta-data -->
        <!-- android:name="android.support.PARENT_ACTIVITY" -->
        <!-- android:value=".activities.MainActivity" /> -->
        <!-- </activity> -->

        <activity
            android:name=".activities.RoboticsEvents"
            android:label="Robotics">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".activities.EventsActivity" />
        </activity>

        <activity
            android:name=".activities.ExpoEvents"
            android:label="Expo">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".activities.EventsActivity" />
        </activity>

        <activity
            android:name=".activities.DebateEvents"
            android:label="Debate">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".activities.EventsActivity" />
        </activity>

        <activity
            android:name=".activities.BuildItEvents"
            android:label="Build It!!">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".activities.EventsActivity" />
        </activity>

        <activity
            android:name=".activities.DesignEvents"
            android:label="Design">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".activities.EventsActivity" />
        </activity>

        <activity
            android:name=".activities.SocialEvents"
            android:label="Social Events">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".activities.EventsActivity" />
        </activity>

        <activity
            android:name=".activities.AboutActivity"
            android:label="@string/about">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".activities.EventsActivity" />
        </activity>

        <activity
            android:name=".activities.TeamActivity"
            android:label="@string/team">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".activities.EventsActivity" />
        </activity>

        <activity
            android:name=".activities.SignInActivity"
            android:label="@string/SignIn">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".activities.MyProfile" />
        </activity>

        <activity
            android:name=".activities.RegisterActivity"
            android:label="@string/SignUp">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".activities.MyProfile" />
        </activity>

        <activity
            android:name=".activities.ScheduleActivity"
            android:label="@string/title_activity_schedule"
            android:parentActivityName=".activities.MainActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.iitp.mayank.celesta2k17.activities.MainActivity" />
        </activity>

        <activity
            android:name=".activities.EventInfoActivity"
            android:theme="@style/AppTheme.NoActionBar" />

        <activity
            android:name=".activities.DevelopersActivity"
            android:parentActivityName=".activities.MainActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.iitp.mayank.celesta2k17.activities.MainActivity" />
        </activity>

        <activity
            android:name=".activities.ImageLauncher"
            android:parentActivityName=".activities.MainActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.iitp.mayank.celesta2k17.activities.MainActivity" />
        </activity>

        <activity android:name=".activities.CodingEvents"
            android:label="Coding">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".activities.EventsActivity" />
        </activity>

        <activity android:name=".activities.PhotographyEvents"
            android:label="Photography Events">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".activities.EventsActivity" />
        </activity>

        <activity android:name=".activities.TreasureHunt"
            android:label="Treasure Hunt">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".activities.EventsActivity" />
        </activity>

        <activity android:name=".activities.StoryWritingEvents"
            android:label="Story Writing">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".activities.EventsActivity" />
        </activity>

        <activity android:name=".activities.QuizWizEvents"
            android:label="Quiz Wiz">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".activities.EventsActivity" />
        </activity>
    </application>

</manifest>